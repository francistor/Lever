{
    "__configDatabaseURL": "someURL",
    "__clientDatabaseURL": "someURL",
    "__eventDatabaseURL": "someURL",
	"port": 9100,
    "databaseOptions":{
        "db": {
            "retryMiliSeconds": 5000,
            "__bufferMaxEntries": "Set to 0 so that, if error, do not try to buffer the operation. Just throw error",
            "bufferMaxEntries": 0
        },
        "server": {
            "poolSize": 1,
            "autoReconnect": true,
            "socketOptions":{
                "connectTimeoutMS": 5000,
                "__socketTimeoutMS": "Represents the time the socket will go away if not used",
                "socketTimeoutMS": 0
            }
        }
    },
    "queryOptions":{
        "timeout": true,
        "maxTimeMS": 5000
    }
}
